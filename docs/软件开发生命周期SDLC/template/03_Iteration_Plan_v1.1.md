# 🚀 迭代计划：v1.1.0 (例如：增加搜索与导出功能)

## 1. 迭代目标
- 实现全局搜索任务功能。
- 支持任务列表导出为 Excel。
- **重构**：优化 v1.0 中硬编码的状态判断逻辑。

## 2. 新需求分析 (Delta)
### 新增功能
- [ ] 搜索框组件 (前端)
- [ ] 搜索接口 (后端：模糊查询 title/content)
- [ ] 导出接口 (后端：EasyExcel)

### 修改功能
- [ ] 任务详情页增加“操作日志” tab。

## 3. 🛑 影响性分析 (架构师核心工作)
> **自问自答**：
> 1. **数据库影响**：
     >    - 需要加索引吗？ -> 是，`title` 和 `content` 字段需加联合索引。
>    - 需要改表结构吗？ -> 否。
>    - 旧数据兼容吗？ -> 是，无需清洗。
> 2. **接口影响**：
     >    - 现有接口参数变了吗？ -> 否，是新增接口。
>    - 返回值结构变了吗？ -> 否。
> 3. **模块依赖**：
     >    - 是否依赖新引入的库？ -> 是，引入 `easyexcel` 和 `lucene` (或 ES)。
>    - 是否影响核心交易链路？ -> 否，搜索是旁路功能。

## 4. 技术债务与重构计划
- **债务项**：v1.0 中 `OrderService` 有 500 行代码，难以维护。
- **本次重构动作**：抽取 `StatusStateMachine` 类，将状态流转逻辑剥离。
- **风险**：重构可能导致状态流转 Bug。
- **应对**：必须补充 `OrderServiceTest` 单元测试，覆盖所有状态流转场景。

## 5. 数据库迁移脚本 (Migration)
- [ ] `V1.1__add_index_to_task.sql` (Flyway/Liquibase 脚本名)

## 6. 回归测试重点
- [ ] 验证原有的“创建任务”功能是否正常。
- [ ] 验证原有的“状态流转”是否正常。
- [ ] 验证新搜索功能在大数据量下的性能。